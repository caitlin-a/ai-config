# create-mcp-guide (personal Cursor skill)

**Location:** `~/.cursor/skills/create-mcp-guide/` — not committed to idp-docs.

**Purpose:** Author `kb_*_mcp_setup_cursor.md` install guides in the [idp-docs](https://dev.azure.com/cfcunderwriting/Technology%20Platform/_git/idp-docs) repo.

**How it was built (Jun 2026):** Calibrated recursively against existing gold guides in `docs/technical-practices/ai-capabilities/mcp-servers/`: generate draft → compare to gold → tune skill files → repeat across 6 MCPs (+ holdouts). Originally a repo skill under `.github/skills/write-mcp-install-guide/`; moved here as a personal skill after abandoning that PR.

**Use:** Open the **idp-docs** workspace, reload Cursor, invoke **create-mcp-guide** in Agent chat.
